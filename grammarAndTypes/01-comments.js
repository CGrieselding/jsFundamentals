// A single line comment

// using // will comment out a single line, making this useful for notes

// We can use CMD + / as a shortcut to make a comment

/* This is a multi-line comment */

/* Using a / and an *
will create
a new multi-line
comment
*/

// In VS code, our shortcut to make a comment will also work within HTML and CSS in the same way, although syntax will be slightly different

// Comments are ignored when running code, anything inside of them will not be run whether it is valid code or otherwise. This makes commenting code very useful when debugging.